# 🔄 Руководство по обновлению проекта на VPS

## Быстрое обновление (после изменений в репозитории)

### 1. Подключитесь к серверу по SSH
```bash
ssh user@your-server-ip
```

### 2. Перейдите в директорию проекта
```bash
cd /var/www/biomorphx
# или путь, где у вас находится проект
```

### 3. Сохраните текущие изменения (если есть локальные правки)
```bash
# Проверьте статус
git status

# Если есть локальные изменения, которые нужно сохранить:
git stash
```

### 4. Получите последние изменения из репозитория
```bash
git pull origin master
```

### 5. Установите новые зависимости (если были добавлены)
```bash
npm install
```

### 6. Пересоберите приложение
```bash
npm run build
```

### 7. Перезапустите приложение через PM2
```bash
# Остановите текущий процесс
pm2 stop biomorphx

# Или перезапустите
pm2 restart biomorphx

# Проверьте статус
pm2 status

# Просмотрите логи (если нужно)
pm2 logs biomorphx
```

### 8. Проверьте работу сайта
```bash
# Проверьте, что приложение запущено
curl http://localhost:3000

# Или откройте в браузере
# http://your-domain.com
```

---

## Полная инструкция (если что-то пошло не так)

### Если возникли проблемы с зависимостями:
```bash
# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Переустановите зависимости
npm install

# Пересоберите
npm run build

# Перезапустите
pm2 restart biomorphx
```

### Если нужно обновить базу данных:
```bash
# Подключитесь к MySQL
sudo mysql -u root -p

# Выберите базу данных
USE biomorphx;

# Выполните необходимые SQL команды (если есть миграции)
# Например:
# ALTER TABLE orders ADD COLUMN new_field VARCHAR(255);
```

### Если нужно обновить переменные окружения:
```bash
# Отредактируйте .env файл
nano .env

# После изменений перезапустите приложение
pm2 restart biomorphx
```

### Если нужно очистить кэш Next.js:
```bash
# Удалите папку .next
rm -rf .next

# Пересоберите
npm run build

# Перезапустите
pm2 restart biomorphx
```

---

## Автоматическое обновление (опционально)

### Создайте скрипт для автоматического обновления:
```bash
nano ~/update-biomorphx.sh
```

Добавьте содержимое:
```bash
#!/bin/bash

cd /var/www/biomorphx

echo "🔄 Обновление проекта..."

# Получаем изменения
git pull origin master

# Устанавливаем зависимости
npm install

# Собираем проект
npm run build

# Перезапускаем приложение
pm2 restart biomorphx

echo "✅ Обновление завершено!"
```

Сделайте скрипт исполняемым:
```bash
chmod +x ~/update-biomorphx.sh
```

Использование:
```bash
~/update-biomorphx.sh
```

---

## Проверка после обновления

1. **Проверьте логи PM2:**
   ```bash
   pm2 logs biomorphx --lines 50
   ```

2. **Проверьте статус PM2:**
   ```bash
   pm2 status
   ```

3. **Проверьте работу сайта:**
   - Откройте главную страницу
   - Проверьте страницу продуктов
   - Проверьте работу корзины
   - Проверьте форму заказа

4. **Проверьте ошибки в консоли браузера:**
   - Откройте DevTools (F12)
   - Проверьте вкладку Console на наличие ошибок

---

## Откат к предыдущей версии (если что-то пошло не так)

```bash
cd /var/www/biomorphx

# Посмотрите историю коммитов
git log --oneline -10

# Откатитесь к предыдущему коммиту (замените COMMIT_HASH)
git checkout COMMIT_HASH

# Пересоберите
npm run build

# Перезапустите
pm2 restart biomorphx
```

Или откатитесь к последнему рабочему коммиту:
```bash
git reset --hard HEAD~1
npm run build
pm2 restart biomorphx
```

---

## Полезные команды PM2

```bash
# Просмотр всех процессов
pm2 list

# Просмотр логов
pm2 logs biomorphx

# Просмотр мониторинга
pm2 monit

# Перезапуск
pm2 restart biomorphx

# Остановка
pm2 stop biomorphx

# Удаление из PM2
pm2 delete biomorphx

# Сохранение конфигурации
pm2 save
```

---

**Дата создания:** 2025  
**Последнее обновление:** после изменений в репозитории

